# Normalise line endings: LF in the repository, native in the working tree.
* text=auto eol=lf

# Explicitly treat binary assets as binary so git never rewrites them.
*.png binary
*.jpg binary
*.ico binary
